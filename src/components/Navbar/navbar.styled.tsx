import styled from 'styled-components';

export const NavbarDiv = styled.div`
  position: absolute;
  top: 40px;
  right: 16px;

  a {
    margin-right: 40px;
  }

  .active {
    font-weight: 600;
    color: #32569b;
  }
`;
